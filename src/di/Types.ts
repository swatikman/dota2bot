export const CONTROLLERS_TYPES = {
    MATCH: Symbol(),
    TEAM: Symbol(),
    TOURNAMENT: Symbol(),
    SUBSCRIPTION: Symbol(),
    USER: Symbol()
};

export const ROUTERS_TYPES = {
    MATCH: Symbol(),
    TEAM: Symbol(),
    TOURNAMENT: Symbol(),
    SUBSCRIPTION: Symbol(),
    USER: Symbol()
};

export const DATABASE_TYPES = {
    MATCH: Symbol(),
    TEAM: Symbol(),
    TOURNAMENT: Symbol(),
    SUBSCRIPTION: Symbol(),
    USER: Symbol()
};

export const RETRIEVERS = {
    API_RETRIEVER: Symbol(),
    TOURNAMENTS: Symbol(),
    TEAMS: Symbol(),
    MATCHES: Symbol()
}

export const NOTIIFICATOR = Symbol();

export const PANDA_API = {
    PANDA: Symbol(),
    TOKEN: Symbol()
};
